# Lessons Learned - Ukiyoe Project

> Claude が間違えた際に追記し、同じミスを防ぐドキュメント。
> セッション開始時に AI が読み込み、過去の失敗パターンを回避する。
>
> CLAUDE.md の「過去の学び」セクションには、特に重要な項目のサマリーも転記する。

---

## 学びの記録

### 2026-02-14: タイムラインのスクロール不具合（5回の修正ループを経て根本解決）

**症状**: `/timeline` で最後の時代を超えて一覧に進めない → 進めるようになったが次は時代1の直後に一覧が表示される、と修正を繰り返した。

**根本原因**: 旧方式（VerticalScroll がホイールを `preventDefault` で奪い、`position: absolute` で全セクションを重ねて opacity で切り替える）の上に、ネイティブスクロール対応のパッチを重ね続けたこと。

- 同一 specificity の CSS クラス（`.timeline-section` と `.timeline-section--native-scroll`）でプロパティを上書きしようとしたが、`position: absolute` が残りセクションがフローに入らなかった
- JSX 側で props（`useNativeScroll`, `scrollProgress`）を追加したつもりが反映されておらず、旧レンダリングパスのままだった
- スクロール量の制限（`maxDelta: 50px`）、遷移クールダウン（800ms）、連続遷移ブロック（3回）といったガード条件が組み合わさり、ユーザーが何十回もスクロールしないと進めない状態になった

**正しい解決**: 旧方式とは **完全に別のクラス名系統** `.timeline-ns` を新設し、CSS cascade の衝突をゼロにした。レンダリングパスも JSX レベルで完全に分岐させた。

**教訓（今後のルール）**:

1. **パッチを重ねない。設計を変えるなら設計ごと切り替える**
   - 既存の CSS クラスに `--modifier` を足して `position` や `overflow` を上書きする方式は、同一 specificity の競合リスクがある。レイアウトモデルが根本的に異なる場合（absolute 重ね vs relative 並べ）は、**クラス名自体を分離**し、旧スタイルの影響を完全に排除する。

2. **1回の修正で直らなかったら、次の修正をする前に根本原因を特定する**
   - 「maxDelta を増やす」「倍率をかける」「クールダウンを短くする」はすべて対症療法。根本原因（ホイールを奪う設計自体）に到達するまで、ブラウザの DevTools やコンソールログで「何が実際に起きているか」を先に確認する。
   - 2回連続で修正が空振りしたら、**修正を止めて原因調査に切り替える**こと。

3. **コンポーネントの props が実際に使われているか確認する**
   - 親が渡す props と、子が受け取る props の不一致は、ビルドエラーにならないが UI が壊れる。変更後は必ず子コンポーネントの関数シグネチャと、その props が JSX 内で参照されていることを目視確認する。

4. **ネイティブスクロールが正解かホイールキャプチャが正解かを最初に決める**
   - 両方の中間はない。ネイティブスクロールなら `preventDefault` は一切しない。ホイールキャプチャなら `position: fixed` + セクション切り替えに徹する。混在させると UX が壊れる。

### 2026-01-09: 作品詳細モーダルの説明文欠落

**症状**: `ArtworkDetailModal.jsx` は説明文表示に対応していたが、説明文が表示されなかった。

**根本原因**: `ukiyoe.js` のデータに `description_ja` / `description_en` フィールドがなかった。

**教訓**: データとUIの整合性を常に確認すること。UIが参照するフィールドがデータソースに存在するか、実装前に確認する。

### 2026-02-24: 記事タイトルの期待値管理

**症状**: 当初のタイトル「AIで浮世絵サイトを作ろうとしたら、手描きに戻った話」では、AIが完全に失敗して100%手作業になったかのような印象を与えてしまう。実態はAIとの協業で、手作業は色塗り部分のみ。

**根本原因**: タイトルの動詞選択が不正確だった。「手描きに戻った」→ 全部手作業の印象。「絵を描く」→ ゼロから描いた印象。実際には「色を塗る」（塗り絵的な作業）。

**教訓**:
1. **タイトルの動詞・名詞は記事内容と正確に一致させる** — 一語の違い（「描く」vs「塗る」）で読者の期待が大きく変わる
2. **タイトルのクリック率だけでなく読後満足度も設計する** — 期待と内容のギャップが「がっかり」にならないよう管理する
3. **AI記事のem dash（—）は避ける** — Medium キュレーターがAI生成テキストの典型パターンとして認識する。カンマ、ピリオド、"but" 等で代替する

### 2026-03-08: レスポンシブタイポグラフィの「見えない変更」問題

**症状**: レスポンシブタイポグラフィシステムを導入したが、ユーザーからデスクトップで「何も変わっていないように見える」とフィードバックがあった。

**根本原因**: 新しい `--type-*` トークンの `clamp()` 最大値が旧トークンの固定値とほぼ同じだった（例: `--type-title-lg` の max 28px = 旧 `--text-2xl` 28px）。変化の大部分はモバイル側で起きており、デスクトップではピクセル単位の微差しかなかった。

**教訓**:

1. **レスポンシブ移行時、デスクトップとモバイルの両方で変化量を事前に計算する** — `clamp()` の min/max がどの画面幅で何px になるかを具体的に計算し、「目に見える変化」がどこで生じるかを予測する。
2. **トークン移行だけでは視覚的改善にならないことがある** — プリミティブ → セマンティックへの移行は保守性の改善であり、ビジュアルの改善が必要な場合はトークンの「段階」自体を変える判断が別途必要。
3. **テキストグループの監査は「グループ内のコントラスト比率」で行う** — 個々のサイズではなく、グループ内の title/body/label の比率（1.4x 以上か？）で判断する。

### 2026-03-09: CSS 変数のサイレント・フォールバック問題

**症状**: `CardPlayground.css` で `var(--color-washi, #ebdcbd)` 、`ArtworkDetailModal.css` で `var(--washi-iro, #f5f0e6)` を使用していたが、`--color-washi` も `--washi-iro` も `:root` に定義されていなかった。フォールバック値で表示が成立していたため、バグとして検出されずに残存。

**根本原因**: CSS カスタムプロパティは未定義でもビルドエラーにならない。`var()` のフォールバック値がある場合、見た目上は正常に動作するため、変数名の typo や存在しない変数への参照が長期間気づかれない。

**教訓**:
1. **CSS 変数名はコピペするか、定義元を grep で確認する** — `--washi` と `--washi-iro`、`--echizen` と `--color-washi` のような「似て非なる名前」は特に危険。
2. **フォールバック値に頼らない** — 変数が確実に定義されている場合、フォールバックは省略するか、省略可能であることを意識する。フォールバックがあると「変数が未定義でも動く」ため、バグが隠れる。
3. **定期的にコードベース全体の CSS 変数監査を行う** — 未定義変数、未使用変数、重複定義は時間とともに蓄積する。

### 2026-03-09: コンポーネント内で定義した関数が useEffect 依存配列に入る問題

**症状**: `LayeredImages.jsx` の `calculateScrollProgress` がコンポーネント本体で定義され、`useEffect` の依存配列に含まれていた。関数は毎レンダーで新しい参照を生成するため、エフェクトが不必要に再実行されていた。

**根本原因**: 純粋関数（state/props に依存しない関数）をコンポーネント本体に書いてしまい、React の参照同一性の仕組みを無視していた。

**教訓**:
1. **state/props に依存しない関数はコンポーネント外に置く** — モジュールスコープで定義すれば参照は不変。
2. **useEffect の依存配列に関数がある場合、その関数の安定性を確認する** — `useCallback` でラップするか、コンポーネント外に移動する。

### 2026-01-05
- （初期作成）
